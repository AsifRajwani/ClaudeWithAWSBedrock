[project]
name = "pythonstarter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest",
    "black",
    "ruff",
    "ipykernel",
    "numpy",
    "pandas",
    "matplotlib",
    "scikit-learn",
    "python-dotenv>=1.2.2",
]
